CREATE TABLE ENTITE (ID INTEGER NOT NULL, PRIMARY KEY (ID))
CREATE TABLE UTILISATEUR (ID INTEGER NOT NULL, DESCRIPTION VARCHAR, EMAIL VARCHAR, NOM VARCHAR, PRENOM VARCHAR, PRIMARY KEY (ID))
CREATE TABLE NOTE (ID INTEGER NOT NULL, COMMENTAIRE VARCHAR, NOTE VARCHAR, ITEM_ID INTEGER, PRIMARY KEY (ID))
CREATE TABLE ITEM (ID INTEGER NOT NULL, DTYPE VARCHAR(31), DESCRIPTION VARCHAR, PRIX INTEGER, TITRE VARCHAR, PRIMARY KEY (ID))
CREATE TABLE LIVRE (ID INTEGER NOT NULL, ISBN VARCHAR, NBPAGE INTEGER, PRIMARY KEY (ID))
CREATE TABLE FILM (ID INTEGER NOT NULL, DUREE INTEGER, GENRE VARCHAR, PRIMARY KEY (ID))
CREATE TABLE UTILISATEUR_NOTE (Utilisateur_ID INTEGER NOT NULL, ListeNote_ID INTEGER NOT NULL, PRIMARY KEY (Utilisateur_ID, ListeNote_ID))
ALTER TABLE NOTE ADD CONSTRAINT FK_NOTE_ITEM_ID FOREIGN KEY (ITEM_ID) REFERENCES ITEM (ID)
ALTER TABLE LIVRE ADD CONSTRAINT FK_LIVRE_ID FOREIGN KEY (ID) REFERENCES ITEM (ID)
ALTER TABLE FILM ADD CONSTRAINT FK_FILM_ID FOREIGN KEY (ID) REFERENCES ITEM (ID)
ALTER TABLE UTILISATEUR_NOTE ADD CONSTRAINT FK_UTILISATEUR_NOTE_Utilisateur_ID FOREIGN KEY (Utilisateur_ID) REFERENCES UTILISATEUR (ID)
ALTER TABLE UTILISATEUR_NOTE ADD CONSTRAINT FK_UTILISATEUR_NOTE_ListeNote_ID FOREIGN KEY (ListeNote_ID) REFERENCES NOTE (ID)
CREATE TABLE SEQUENCE (SEQ_NAME VARCHAR(50) NOT NULL, SEQ_COUNT NUMERIC(38), PRIMARY KEY (SEQ_NAME))
INSERT INTO SEQUENCE(SEQ_NAME, SEQ_COUNT) values ('SEQ_GEN', 0)
